var a=1;

let namesSet = new Set(['a', 'b', 'c', 'd']);


var b=Array.from(namesSet);
console.log(b);

var c=3+4;

var c='template String';
$('.template-string').html(`这是<span>${c}</span>`)

export default a;
